//beschleunigt player//zerbricht wenn der spieler schnell dagegen kommtfunction aIce(){	this.active = 0;}aIce.prototype.onUpdate = function(ts){	if(gGlobals.player == 0)		return;		this.pos = {x: this.ent.object.pos.x+32, y: this.ent.object.pos.y+64};	var dir = {x : gGlobals.player.object.pos.x+48-this.pos.x, y : gGlobals.player.object.pos.y-this.pos.y};	var dist = dir.x*dir.x+dir.y*dir.y;	dist = Math.sqrt(dist);	dir.y /= dist;	if(dist < 64 && gGlobals.player.action.health > 0)	{		if(this.active == 0)		{			this.active = 1;			gGlobals.player.action.onIce += 1.0;		}	}	else	{		if(this.active == 1)		{			this.active = 0;			gGlobals.player.action.onIce -= 1.0;		}	}}